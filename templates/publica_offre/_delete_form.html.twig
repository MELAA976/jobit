<form method="post" action="{{ path('app_publica_offre_delete', {'id': publica_offre.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ publica_offre.id) }}">
    <button class="btn">Delete</button>
</form>
