<turbo-frame id="delPostu">

<form method="post" action="{{ path('app_offre_user_delete', {'id': offre_user.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ offre_user.id) }}">
    <button class="btn">Delete</button>
</form>

</turbo-frame>
